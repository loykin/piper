apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: piper
resources:
  - namespace.yaml
  - rbac.yaml
  - server.yaml
configMapGenerator:
  - name: piper-config
    files:
      - piper.yaml=piper.yaml
