openapi: 3.1.0
info:
  title: piper API
  version: 0.1.0
  description: |
    Project-owned resources are always exposed below `/api/projects/{project_id}`.
    Collection POST operations return `201 Created`, successful mutations with no
    response body return `204 No Content`, JSON field names use snake_case, and
    errors use the shape `{"error":"message"}`.
servers:
  - url: http://localhost:8080
security:
  - bearerAuth: []
paths:
  /health:
    get:
      security: []
      responses:
        "200":
          description: Server health.
  /events:
    get:
      responses:
        "200":
          description: Server-sent event stream for run, step, schedule, and service events.
          content:
            text/event-stream:
              schema:
                type: string
  /api/projects/{project_id}/credentials:
    get:
      parameters:
        - $ref: "#/components/parameters/ProjectID"
      responses:
        "200":
          description: Project credential metadata. Secret values are never returned.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Credential" }
    post:
      parameters:
        - $ref: "#/components/parameters/ProjectID"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateCredentialRequest" }
      responses:
        "201":
          description: Created credential metadata.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Credential" }
        "409":
          description: Credential already exists.
  /api/projects/{project_id}/credentials/{name}:
    get:
      parameters:
        - $ref: "#/components/parameters/ProjectID"
        - $ref: "#/components/parameters/CredentialName"
      responses:
        "200":
          description: Project credential metadata.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Credential" }
        "404":
          description: Credential not found.
    patch:
      parameters:
        - $ref: "#/components/parameters/ProjectID"
        - $ref: "#/components/parameters/CredentialName"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/PatchCredentialRequest" }
      responses:
        "200":
          description: Credential metadata updated.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Credential" }
        "404":
          description: Credential not found.
    delete:
      parameters:
        - $ref: "#/components/parameters/ProjectID"
        - $ref: "#/components/parameters/CredentialName"
      responses:
        "204":
          description: Credential deleted.
        "404":
          description: Credential not found.
  /api/projects/{project_id}/credentials/{name}/rotate:
    post:
      parameters:
        - $ref: "#/components/parameters/ProjectID"
        - $ref: "#/components/parameters/CredentialName"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RotateCredentialRequest" }
      responses:
        "204":
          description: Credential value rotated.
        "404":
          description: Credential not found.
        "409":
          description: Credential is disabled.
  /api/projects/{project_id}/credentials/{name}/test:
    post:
      parameters:
        - $ref: "#/components/parameters/ProjectID"
        - $ref: "#/components/parameters/CredentialName"
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/TestCredentialRequest" }
      responses:
        "200":
          description: Credential test succeeded.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TestCredentialResult" }
        "502":
          description: Credential test failed.
  /api/projects/{project_id}/alert-rules:
    get:
      parameters:
        - $ref: "#/components/parameters/ProjectID"
        - { name: limit, in: query, schema: { type: integer, minimum: 1 } }
        - { name: offset, in: query, schema: { type: integer, minimum: 0 } }
      responses:
        "200":
          description: Project alert rules.
          headers:
            X-Total-Count: { schema: { type: integer } }
          content:
            application/json:
              schema: { type: array, items: { $ref: "#/components/schemas/AlertRule" } }
    post:
      parameters:
        - $ref: "#/components/parameters/ProjectID"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateAlertRuleRequest" }
      responses:
        "201":
          description: Alert rule created.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AlertRule" }
        "400": { description: Invalid condition, cooldown, or notification credential. }
        "409": { description: Rule name already exists. }
  /api/projects/{project_id}/alert-rules/{id}:
    get:
      parameters:
        - $ref: "#/components/parameters/ProjectID"
        - $ref: "#/components/parameters/AlertRuleID"
      responses:
        "200":
          description: Alert rule.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AlertRule" }
        "404": { description: Alert rule not found. }
    patch:
      parameters:
        - $ref: "#/components/parameters/ProjectID"
        - $ref: "#/components/parameters/AlertRuleID"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/PatchAlertRuleRequest" }
      responses:
        "200":
          description: Alert rule updated.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AlertRule" }
        "400": { description: Invalid rule. }
        "404": { description: Alert rule not found. }
        "409": { description: Rule name already exists. }
    delete:
      parameters:
        - $ref: "#/components/parameters/ProjectID"
        - $ref: "#/components/parameters/AlertRuleID"
      responses:
        "204": { description: Alert rule deleted. }
        "404": { description: Alert rule not found. }
  /api/projects/{project_id}/runs:
    parameters:
      - $ref: "#/components/parameters/ProjectID"
    get:
      parameters:
        - name: status
          in: query
          schema: { type: string }
        - name: pipeline_name
          in: query
          schema: { type: string }
      responses:
        "200":
          description: Runs. Step summaries are included only when include_steps=true.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/RunWithSteps" }
    post:
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [yaml]
              properties:
                yaml: { type: string }
                params: { type: object, additionalProperties: true }
                experiment: { type: string }
                vars: { type: object, additionalProperties: true }
      responses:
        "201":
          description: Created run id.
          content:
            application/json:
              schema:
                type: object
                properties:
                  run_id: { type: string }
  /api/projects/{project_id}/runs/{id}:
    parameters:
      - $ref: "#/components/parameters/ProjectID"
    get:
      parameters:
        - $ref: "#/components/parameters/RunID"
      responses:
        "200":
          description: Run detail.
          content:
            application/json:
              schema:
                type: object
                properties:
                  run: { $ref: "#/components/schemas/Run" }
                  steps:
                    type: array
                    items: { $ref: "#/components/schemas/Step" }
    delete:
      parameters:
        - $ref: "#/components/parameters/RunID"
      responses:
        "204":
          description: Deleted.
  /api/projects/{project_id}/runs/{id}/cancel:
    parameters:
      - $ref: "#/components/parameters/ProjectID"
    post:
      parameters:
        - $ref: "#/components/parameters/RunID"
      responses:
        "200":
          description: Run canceled.
  /api/projects/{project_id}/runs/{id}/rerun:
    parameters:
      - $ref: "#/components/parameters/ProjectID"
    post:
      parameters:
        - $ref: "#/components/parameters/RunID"
      responses:
        "201":
          description: Replacement run created.
          content:
            application/json:
              schema:
                type: object
                required: [run_id]
                properties:
                  run_id: { type: string }
  /api/projects/{project_id}/runs/{id}/steps/{step}/retry:
    parameters:
      - $ref: "#/components/parameters/ProjectID"
    post:
      parameters:
        - $ref: "#/components/parameters/RunID"
        - name: step
          in: path
          required: true
          schema: { type: string }
      responses:
        "201":
          description: Retry run created.
          content:
            application/json:
              schema:
                type: object
                required: [run_id]
                properties:
                  run_id: { type: string }
  /api/projects/{project_id}/runs/{id}/artifacts:
    parameters:
      - $ref: "#/components/parameters/ProjectID"
    get:
      parameters:
        - $ref: "#/components/parameters/RunID"
      responses:
        "200":
          description: Artifact tree.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/StepArtifacts" }
  /api/projects/{project_id}/runs/{id}/steps/{step}/logs:
    parameters:
      - $ref: "#/components/parameters/ProjectID"
    get:
      parameters:
        - $ref: "#/components/parameters/RunID"
        - name: step
          in: path
          required: true
          schema: { type: string }
        - name: after_id
          in: query
          deprecated: true
          schema: { type: integer, format: int64 }
        - name: cursor
          in: query
          description: Opaque cursor returned by X-Next-Cursor.
          schema: { type: string }
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 5000, default: 1000 }
        - name: since
          in: query
          schema: { type: string, format: date-time }
        - name: until
          in: query
          schema: { type: string, format: date-time }
        - name: search
          in: query
          description: Available only when the project Member advertises full_text_search.
          schema: { type: string }
      responses:
        "200":
          description: Step logs.
          headers:
            X-Next-Cursor:
              description: Opaque cursor for the next page; absent on the final page.
              schema: { type: string }
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/LogLine" }
  /api/projects/{project_id}/runs/{id}/steps/{step}/logs/stream:
    parameters:
      - $ref: "#/components/parameters/ProjectID"
    get:
      parameters:
        - $ref: "#/components/parameters/RunID"
        - name: step
          in: path
          required: true
          schema: { type: string }
        - name: after_id
          in: query
          description: Resume after this log ID when Last-Event-ID is not present.
          schema: { type: integer, format: int64 }
        - name: Last-Event-ID
          in: header
          description: Opaque SSE resume cursor sent automatically by EventSource.
          schema: { type: string }
      responses:
        "200":
          description: >-
            Server-sent step log stream. Default messages contain LogLine JSON;
            each log frame has an SSE id for automatic resume;
            done marks normal completion; stats_error contains
            {code,message,retryable} JSON for statistics backend failures.
          content:
            text/event-stream:
              schema: { type: string }
  /api/projects/{project_id}/stats/capabilities:
    parameters:
      - $ref: "#/components/parameters/ProjectID"
    get:
      responses:
        "200":
          description: Statistics features exposed by the Member that owns this project.
          content:
            application/json:
              schema:
                type: object
                required: [full_text_search, time_range, metric_key_filter, healthy, degraded, pending_bytes]
                properties:
                  full_text_search: { type: boolean }
                  time_range: { type: boolean }
                  metric_key_filter: { type: boolean }
                  healthy: { type: boolean }
                  degraded: { type: boolean }
                  pending_bytes: { type: integer, format: int64, minimum: 0 }
                  last_error: { type: string }
  /api/projects/{project_id}/runs/{id}/metrics:
    parameters:
      - $ref: "#/components/parameters/ProjectID"
    get:
      parameters:
        - $ref: "#/components/parameters/RunID"
        - name: step
          in: query
          schema: { type: string }
        - name: key
          in: query
          description: Repeat to select multiple metric keys.
          schema: { type: array, items: { type: string } }
          style: form
          explode: true
        - name: cursor
          in: query
          schema: { type: string }
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 5000, default: 1000 }
        - name: since
          in: query
          schema: { type: string, format: date-time }
        - name: until
          in: query
          schema: { type: string, format: date-time }
      responses:
        "200":
          description: Run metrics.
          headers:
            X-Next-Cursor:
              description: Opaque cursor for the next page; absent on the final page.
              schema: { type: string }
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
  /api/projects/{project_id}/schedules:
    parameters:
      - $ref: "#/components/parameters/ProjectID"
    get:
      responses:
        "200":
          description: Schedules.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Schedule" }
    post:
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, yaml, type]
              properties:
                name: { type: string }
                yaml: { type: string }
                type: { type: string, enum: [immediate, once, cron] }
                cron: { type: string }
                run_at: { type: string, format: date-time }
                params: { type: object, additionalProperties: true }
                max_runs: { type: integer, minimum: 0 }
      responses:
        "201":
          description: Created schedule id.
          content:
            application/json:
              schema:
                type: object
                required: [schedule_id]
                properties:
                  schedule_id: { type: string }
  /api/projects/{project_id}/schedules/{id}:
    parameters:
      - $ref: "#/components/parameters/ProjectID"
    get:
      parameters:
        - $ref: "#/components/parameters/ScheduleID"
      responses:
        "200":
          description: Schedule detail.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Schedule" }
    patch:
      parameters:
        - $ref: "#/components/parameters/ScheduleID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [enabled]
              properties:
                enabled: { type: boolean }
      responses:
        "200":
          description: Enabled flag updated.
    delete:
      parameters:
        - $ref: "#/components/parameters/ScheduleID"
      responses:
        "204":
          description: Deleted.
  /api/projects/{project_id}/schedules/{id}/backfill:
    parameters:
      - $ref: "#/components/parameters/ProjectID"
    post:
      parameters:
        - $ref: "#/components/parameters/ScheduleID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [from, to]
              properties:
                from: { type: string, format: date-time }
                to: { type: string, format: date-time }
      responses:
        "201":
          description: Backfill runs created.
          content:
            application/json:
              schema:
                type: object
                required: [run_ids]
                properties:
                  run_ids:
                    type: array
                    items: { type: string }
  /api/projects/{project_id}/pipelines:
    parameters:
      - $ref: "#/components/parameters/ProjectID"
    get:
      responses:
        "200":
          description: Versioned pipeline templates.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/PipelineTemplate" }
    post:
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [yaml]
              properties:
                yaml: { type: string }
                volume_id: { type: string }
      responses:
        "201":
          description: Pipeline template version created.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PipelineTemplate" }
  /api/projects/{project_id}/pipelines/{id}:
    parameters:
      - $ref: "#/components/parameters/ProjectID"
      - name: id
        in: path
        required: true
        schema: { type: string }
    get:
      responses:
        "200":
          description: Pipeline template version.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PipelineTemplate" }
    delete:
      responses:
        "204": { description: Pipeline template version deleted. }
  /api/projects/{project_id}/notebooks:
    parameters:
      - $ref: "#/components/parameters/ProjectID"
    get:
      responses:
        "200":
          description: Notebook servers.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/NotebookServer" }
    post:
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [yaml]
              properties:
                yaml: { type: string }
                volume_id: { type: string }
      responses:
        "201":
          description: Notebook server created.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/NotebookServer" }
  /api/projects/{project_id}/notebooks/{name}:
    parameters:
      - $ref: "#/components/parameters/ProjectID"
      - name: name
        in: path
        required: true
        schema: { type: string }
    get:
      responses:
        "200":
          description: Notebook server.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/NotebookServer" }
    delete:
      responses:
        "204": { description: Notebook server deleted and volume released. }
  /api/projects/{project_id}/notebooks/{name}/start:
    parameters:
      - $ref: "#/components/parameters/ProjectID"
      - name: name
        in: path
        required: true
        schema: { type: string }
    post:
      responses:
        "204": { description: Notebook start requested. }
  /api/projects/{project_id}/notebooks/{name}/stop:
    parameters:
      - $ref: "#/components/parameters/ProjectID"
      - name: name
        in: path
        required: true
        schema: { type: string }
    post:
      responses:
        "204": { description: Notebook stop requested. }
  /api/projects/{project_id}/services:
    parameters:
      - $ref: "#/components/parameters/ProjectID"
    get:
      responses:
        "200":
          description: Deployed services.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Service" }
    post:
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [yaml]
              properties:
                yaml: { type: string }
      responses:
        "201":
          description: Deployed service.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Service" }
  /api/projects/{project_id}/services/{name}:
    parameters:
      - $ref: "#/components/parameters/ProjectID"
    get:
      parameters:
        - name: name
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Service detail.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Service" }
    delete:
      parameters:
        - name: name
          in: path
          required: true
          schema: { type: string }
      responses:
        "204":
          description: Stopped and deleted.
  /api/projects/{project_id}/services/{name}/restart:
    parameters:
      - $ref: "#/components/parameters/ProjectID"
    post:
      parameters:
        - name: name
          in: path
          required: true
          schema: { type: string }
      responses:
        "204":
          description: Restarted.
  /api/projects/{project_id}/runs/{id}/artifacts/{step}/{artifact}/view:
    parameters:
      - $ref: "#/components/parameters/ProjectID"
      - $ref: "#/components/parameters/RunID"
      - name: step
        in: path
        required: true
        schema: { type: string }
      - name: artifact
        in: path
        required: true
        schema: { type: string }
    post:
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [type]
              properties:
                type: { type: string }
      responses:
        "201":
          description: Viewer created.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Viewer" }
        "200":
          description: Existing running viewer returned.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Viewer" }
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
  parameters:
    ProjectID:
      name: project_id
      in: path
      required: true
      schema: { type: string }
    CredentialName:
      name: name
      in: path
      required: true
      schema: { type: string }
    AlertRuleID:
      name: id
      in: path
      required: true
      schema: { type: string, format: uuid }
    RunID:
      name: id
      in: path
      required: true
      schema: { type: string }
    ScheduleID:
      name: id
      in: path
      required: true
      schema: { type: string }
  schemas:
    Credential:
      type: object
      properties:
        name: { type: string }
        kind: { type: string, enum: [generic, git, s3, gcs, azure, slack, webhook, mlflow] }
        endpoint: { type: string }
        keys:
          type: array
          items: { type: string }
        disabled: { type: boolean }
        last_used_at: { type: string, format: date-time }
        last_tested_at: { type: string, format: date-time }
        last_test_ok: { type: boolean }
        last_test_message: { type: string }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    CreateCredentialRequest:
      type: object
      required: [name, kind, data]
      properties:
        name: { type: string }
        kind: { type: string, enum: [generic, git, s3, gcs, azure, slack, webhook, mlflow] }
        endpoint: { type: string }
        data:
          type: object
          additionalProperties: { type: string }
    RotateCredentialRequest:
      type: object
      required: [data]
      properties:
        data:
          type: object
          additionalProperties: { type: string }
    PatchCredentialRequest:
      type: object
      properties:
        enabled: { type: boolean }
        endpoint: { type: string }
    TestCredentialRequest:
      type: object
      properties:
        repo: { type: string }
    TestCredentialResult:
      type: object
      properties:
        ok: { type: boolean }
        message: { type: string }
    AlertRule:
      type: object
      required: [id, project_id, name, on, notify, cooldown_seconds, enabled, created_at, updated_at]
      properties:
        id: { type: string, format: uuid }
        project_id: { type: string }
        name: { type: string }
        on: { type: string, enum: [event, metric] }
        event_type: { type: string, enum: [run.completed, service.status, notebook.running, notebook.stopped, notebook.failed] }
        when: { type: string, description: 'Optional strict comparison such as fields.status == "failed".' }
        metric_key: { type: string }
        condition: { type: string, description: "Numeric comparison such as < 0.8." }
        notify: { type: array, minItems: 1, items: { type: string } }
        cooldown_seconds: { type: integer, minimum: 10 }
        enabled: { type: boolean }
        created_by: { type: string }
        last_matched_at: { type: string, format: date-time }
        last_attempted_at: { type: string, format: date-time }
        last_success_at: { type: string, format: date-time }
        last_error: { type: string }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    CreateAlertRuleRequest:
      type: object
      required: [name, on, notify]
      properties:
        name: { type: string }
        on: { type: string, enum: [event, metric] }
        event_type: { type: string }
        when: { type: string }
        metric_key: { type: string }
        condition: { type: string }
        notify: { type: array, minItems: 1, items: { type: string } }
        cooldown_seconds: { type: integer, minimum: 10 }
        enabled: { type: boolean, default: true }
    PatchAlertRuleRequest:
      type: object
      properties:
        name: { type: string }
        on: { type: string, enum: [event, metric] }
        event_type: { type: string }
        when: { type: string }
        metric_key: { type: string }
        condition: { type: string }
        notify: { type: array, minItems: 1, items: { type: string } }
        cooldown_seconds: { type: integer, minimum: 10 }
        enabled: { type: boolean }
    Run:
      type: object
      properties:
        id: { type: string }
        project_id: { type: string }
        schedule_id: { type: string }
        experiment: { type: string }
        pipeline_name: { type: string }
        status: { type: string, enum: [scheduled, running, success, failed, canceled] }
        started_at: { type: string, format: date-time }
        ended_at: { type: string, format: date-time }
        scheduled_at: { type: string, format: date-time }
        pipeline_yaml: { type: string }
        params_json: { type: string }
        created_by: { type: string }
    RunWithSteps:
      allOf:
        - $ref: "#/components/schemas/Run"
        - type: object
          properties:
            steps:
              type: array
              items: { $ref: "#/components/schemas/Step" }
    Step:
      type: object
      properties:
        run_id: { type: string }
        step_name: { type: string }
        status: { type: string }
        started_at: { type: string, format: date-time }
        ended_at: { type: string, format: date-time }
        attempts: { type: integer }
        error: { type: string }
    Schedule:
      type: object
      properties:
        id: { type: string }
        project_id: { type: string }
        name: { type: string }
        pipeline_yaml: { type: string }
        template_version_id: { type: string }
        schedule_type: { type: string, enum: [immediate, once, cron] }
        cron_expr: { type: string }
        enabled: { type: boolean }
        max_runs: { type: integer }
        params_json: { type: string }
        last_run_at: { type: string, format: date-time }
        next_run_at: { type: string, format: date-time }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    Service:
      type: object
      properties:
        project_id: { type: string }
        name: { type: string }
        run_id: { type: string }
        artifact: { type: string }
        status: { type: string, enum: [starting, running, stopping, stopped, failed] }
        endpoint: { type: string }
        namespace: { type: string }
        pid: { type: integer }
        runtime_id: { type: string }
        yaml: { type: string }
        created_by: { type: string }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    PipelineTemplate:
      type: object
      properties:
        id: { type: string }
        project_id: { type: string }
        name: { type: string }
        version: { type: integer }
        description: { type: string }
        tags: { type: array, items: { type: string } }
        yaml: { type: string }
        snapshot_id: { type: string }
        volume_id: { type: string }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    NotebookServer:
      type: object
      properties:
        project_id: { type: string }
        name: { type: string }
        status: { type: string, enum: [provisioning, starting, running, stopping, stopped, failed] }
        env: { type: string }
        image: { type: string }
        endpoint: { type: string }
        work_dir: { type: string }
        runtime_id: { type: string }
        volume_id: { type: string }
        yaml: { type: string }
        created_by: { type: string }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    Viewer:
      type: object
      properties:
        id: { type: string }
        project_id: { type: string }
        type: { type: string }
        run_id: { type: string }
        step_name: { type: string }
        artifact: { type: string }
        status: { type: string, enum: [starting, running, failed, stopped] }
        endpoint: { type: string }
        url: { type: string }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
        expires_at: { type: string, format: date-time }
    LogLine:
      type: object
      properties:
        id: { type: integer, format: int64 }
        event_id: { type: string }
        project_id: { type: string }
        run_id: { type: string }
        step_name: { type: string }
        ts: { type: string, format: date-time }
        stream: { type: string, enum: [stdout, stderr] }
        line: { type: string }
    StepArtifacts:
      type: object
      properties:
        step: { type: string }
        artifacts:
          type: array
          items:
            type: object
            properties:
              name: { type: string }
              files:
                type: array
                items:
                  type: object
                  properties:
                    path: { type: string }
                    size: { type: integer, format: int64 }
                    modified_at: { type: string, format: date-time }
    ModelService:
      type: object
      properties:
        apiVersion: { type: string }
        kind: { type: string }
        metadata:
          type: object
          properties:
            name: { type: string }
        spec:
          type: object
          properties:
            model:
              type: object
              properties:
                from_artifact:
                  type: object
                  properties:
                    pipeline: { type: string }
                    step: { type: string }
                    artifact: { type: string }
                    run: { type: string }
                from_uri:
                  type: string
                  description: file://, http://, https://, or s3:// model location.
